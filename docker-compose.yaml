services:
  app:
    build:
      dockerfile: ./Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "200k"
        max-file: "3"
